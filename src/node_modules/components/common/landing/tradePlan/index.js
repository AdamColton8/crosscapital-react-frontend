import React from 'react';
import {Link} from "react-router";

const TradePlanItem = ({plan})=> {
  return (
    <div className="col-12 col-md-6 col-lg-4 mb-3 mb-lg-0">
      <div className="plan__item h-100">
        <div className="h2 text-dark mb-4 font-weight-light text-capitalize">{plan.name}</div>
        <p className="plan-descr text-secondary mb-3">{plan.description}</p>
        <div className="plan__values">
          <div className="h4 text-success">{plan.term} days</div>
          <div className="h4 text-success">{plan.percent} %</div>
          <div className="h4">
            <span>From </span>
            <span className="text-success">
              {plan.deposit_min}
            </span>
            <span> to </span>
            <span className="text-success">
              {plan.deposit_max} €
            </span>
          </div>
        </div>
        <div className="text-secondary plan-descr">
          <p className="mb-0">- Your deposit is included in your payments</p>
          <p className="mb-0">- Minimum withdrawal amount 10 euro</p>
          <p className="mb-0">- Pays out Monday to Friday</p>
        </div>
        <Link className="btn btn-success btn-lg plan-invest__btn w-100" to='/auth/login'> Invest now </Link>
      </div>
    </div>
  );
};

export default TradePlanItem
