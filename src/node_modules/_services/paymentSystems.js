import axios from 'axios';
import requestsUrl from 'projConfig'

export const paymentSystemService = {
  fetchPaymentData,
};

function fetchPaymentData(aliasObj) {
  return axios.post(requestsUrl.paymentSystemReplenishData, aliasObj);
}