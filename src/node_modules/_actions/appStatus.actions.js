import { appStatusConstants } from '../_constants';

export const appStatusActions = {
  profileSidebar,
};

function profileSidebar(flag) {
  let payload = {
    profileSidebar: flag
  };
  return { type: appStatusConstants.PROFILE_SIDEBAR, payload: payload };
}
