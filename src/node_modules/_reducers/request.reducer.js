import { requestConstants } from '../_constants';

export function request(state = false, action) {
  switch (action.type) {
    case requestConstants.IN_PROCESS:
      return true;
    case requestConstants.COMPLETED:
      return false;
    default:
      return state
  }
}