import { tradePlansConstants } from '../_constants';

export function tradePlans(state = [], {type, payload}) {
  switch (type) {
    case tradePlansConstants.REQUEST:
      return state;
    case tradePlansConstants.SUCCESS:
      return payload;
    default:
      return state
  }
}